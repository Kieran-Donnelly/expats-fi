import type { ReactNode } from 'react'

import 'leaflet/dist/leaflet.css'
import '../../eats.css'

export default function AreaGuideLayout({ children }: { children: ReactNode }) {
  return children
}
